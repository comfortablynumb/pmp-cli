output "deployment_name" {
  description = "Name of the Kubernetes deployment"
  value       = kubernetes_deployment.{{name}}.metadata[0].name
}

output "namespace" {
  description = "Namespace where the deployment is created"
  value       = "{{namespace}}"
}

output "environment" {
  description = "Environment tag"
  value       = "{{environment}}"
}
