{{#if registry_password}}
apiVersion: v1
kind: Secret
metadata:
  name: {{name}}-registry-secret
  namespace: {{namespace}}
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: {{registry_password}}
{{/if}}
